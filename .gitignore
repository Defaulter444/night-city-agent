node_modules/
# Только в корне: у LevelDB свежие записи лежат в файле вида `000103.log`
# внутри пака, и правило `*.log` уносило бы их мимо репозитория и мимо
# релиза — компендиум уезжал бы пользователю без последних записей.
/*.log
.DS_Store
Thumbs.db
night-city-agent.zip
